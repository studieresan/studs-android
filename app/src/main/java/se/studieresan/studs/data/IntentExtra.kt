package se.studieresan.studs.data

private const val PREFIX = "se.studieresan"

object IntentExtra {
    const val EMAIL = "$PREFIX.Email"
    const val EVENT = "$PREFIX.Event"
    const val PRE_EVENT_FORM_FIELDS = "$PREFIX.PreEventFormField"
    const val POST_EVENT_FORM_FIELDS = "$PREFIX.PostEventFormField"
    const val TRIP_INFO_TYPE = "$PREFIX.TripInfoType"
}
